# CrawlIQ Handoff — 2026-05-02

> Active session was compacted twice; this file is the full handoff for the next chat. Read it once at the start, then continue from "Open issues — START HERE" without asking the user to repeat.

---

## TL;DR for the next agent

You're working on **CrawlIQ** at `/Users/laptopchoice/Projects/crawliq` — a Next.js 16 App Router AI website-audit marketing site (cream editorial theme, tangerine `#FF5E1A` accent, Figtree + Instrument Serif italic + Geist Mono).

**User is Umer Farooq (Pakistan).** Speaks Hinglish/English mix. Direct, irritated when work is half-done or silently reverted. Wants execution, not menu prompts. Auto mode is on.

**Latest preview deploy (at time of writing):** https://crawliq-dpkj22xj7-umidx124s-projects.vercel.app
> ⚠ This URL becomes stale after the next push. Run `vercel ls | head -5` to get the current latest.

**Latest commit:** `8c2e19c` — Fix hero floating-cards visibility — z-index + reposition

**User's last words this session:**
1. "audit ke results dekho" — go look at audit report
2. "is k ilawa bhi bht issues agaye site mein" — apart from this many other issues have come on the site
3. "stop"
4. "new chat k liye context de do wo ker le ga ye session 2 bar compact ker liya he"

So: **don't keep working**. Let user describe specific issues in the next session. Use this context to skip explanation overhead and act fast.

---

## Project facts

| Thing | Value |
|---|---|
| Path | `/Users/laptopchoice/Projects/crawliq` |
| Framework | Next.js 16 App Router + Turbopack |
| Lang | TypeScript strict |
| CSS | Tailwind v4 + CSS vars in `src/app/globals.css` |
| Auth | Clerk (`@clerk/nextjs`) |
| **DB** | **Neon Postgres via Prisma** — `prisma generate` runs in `pnpm build`; without `DATABASE_URL` build fails |
| Fonts | Figtree (display/sans), Instrument Serif (italic accent), Geist Mono (numbers/labels) |
| 3D | React Three Fiber + drei (`src/components/three-scene-hero.tsx`) |
| Motion | framer-motion (no GSAP) |
| Icons | `@phosphor-icons/react` ONLY (no emojis) |
| Package mgr | pnpm |
| **Billing** | **Stripe** — Pro/Agency monthly+annual price IDs, webhook at `/api/stripe/webhook` |
| **Email** | **Resend** — pilot leads + audit reports; from `audit@crawliq.ai` |
| **Real-data sources** | Google PageSpeed Insights, Chrome UX Report (CrUX), OpenPageRank, Google Search Console (OAuth) — these are why "no fake findings" rule is enforceable |
| **Cron** | Vercel Cron at `/api/cron/*`, auth via `CRON_SECRET` |
| Vercel project | `crawliq` (team: `umidx124s-projects`, id `prj_Qw9mTUGZS7dMzrqvm8oxnjrkiULO`) |
| Vercel CLI | 52.0.0 (warn: 53 available, user hasn't upgraded) |
| **Custom domain** | **NONE attached yet.** `vercel domains ls` only shows `digitalpointllc.com` + `virtualcustomersolution.com`. CrawlIQ runs on Vercel auto-aliases only. Env says `crawliq.ai` but it's not registered/attached. |

---

## Theme tokens — LOCKED. Do not silently revert.

```css
/* globals.css @theme */
--color-bg: #EFE5D0;        /* warm paper cream — NOT off-white. Was #F5F0E8 earlier, deepened. */
--color-bg-2: #E5D9BF;      /* section bands */
--color-bg-3: #DBCEB1;      /* raised wells */
--color-surface: #F8EFD8;   /* lifted cards — luminous cream */
--color-fg: #1A1612;        /* warm near-black, NOT pure black */
--color-fg-muted: #5C544A;
--color-fg-subtle: #877E72;
--color-fg-faint: #B5AC9F;
--color-border: rgb(26 22 18 / 0.10);
--color-border-strong: rgb(26 22 18 / 0.22);
--color-accent: #FF5E1A;    /* electric tangerine */
--color-accent-hover: #E84F0F;
--color-accent-soft: rgb(255 94 26 / 0.12);
--color-accent-fg: #FFFFFF;
--color-pass: #047857;
--color-warn: #B45309;
--color-crit: #B91C1C;
```

Banned:
- Pure white background. Pure black text. Purple, indigo, violet anywhere.
- "Inter" font (use Figtree).
- Generic shadow stacks — use `.shadow-layered*` utilities instead.
- Emojis in markup.

---

## Recent commits (most recent first)

```
8c2e19c Fix hero floating-cards visibility — z-index + reposition
c3972f0 Editorial elevation sweep — Instrument Serif voice, layered chrome  (28 files)
cdb041f Hero floating-cards layer + typography/shadow/motion system
a72fc6b Fix nav banner + pricing badge clip on cream theme
c86c5bd Final ship: tangerine theme + 3D audit-stamps scene live on homepage
ce72d04 Polish pass: audit-stamp 3D scene, global cursor, fixes
13b9d8f Ship 10 innovation phases as isolated /preview/* routes
```

---

## What was just shipped (this session)

### 1. Nav header dark-slab fix
`src/components/sections/nav.tsx` line 50-52
- Was `backgroundColor: rgb(14 22 32 / 0.85)` (dark navy) on scroll → looked like a black band slammed across cream page
- Now `rgb(239 229 208 / 0.88)` (cream translucent) + backdrop-blur + soft border-strong + drop shadow
- At top (no scroll): transparent

### 2. Pricing "Most popular" badge clip fix
`src/components/sections/pricing.tsx` line 187, 195
- Was: `<article className="… overflow-hidden">` — clipped the badge that sat at `top-0 -translate-y-1/2`
- Now: `overflow-hidden` moved to the radial-glow div with `rounded-[inherit]` so glow stays clipped inside corners but badge is fully visible

### 3. Hero floating-cards layer (NEW component)
`src/components/hero-floating-cards.tsx` — 290 lines
- 8 audit-themed cards drift behind the H1: Score chips, Finding cards (crit/warn/pass), Status codes (200/301), Metric chips (LCP/CLS), Signal chips (canonical/schema.org)
- Positions densify left/text half + bottom-band, only 2 small chips peek into right side (avoiding 3D scene)
- Motion: nano-drift Y 12-22px on 14-22s loop, opacity 0.78↔1 pulse 6-9s, ±1.4° rotate
- Plus `<HeroLiveFinding>` single big-motion card top-center re-printing one of 6 findings every 4.2s

### 4. Innovative typography utilities (globals.css)
- `.drop-cap` — Instrument Serif italic 5.4em first-letter, accent-colored, float-left
- `.pull-quote` — magazine-bordered serif italic
- `.display-3xl/2xl/xl/lg` — clamp() ladder with consistent letter-spacing
- `.voice-italic`, `.word-mix`, `.mono-tag`, `.num-display`

### 5. Layered shadow system (globals.css)
- `.shadow-layered-sm/md/lg/xl` — multi-stop: top inner highlight + contact + mid + long ambient
- `.shadow-layered-hover` — ambient lift transition
- `.shadow-accent` — accent-tinted halo for featured/active
- `.shadow-well` — inset for inputs

### 6. Multi-scale motion (globals.css)
- Nano: `.motion-nano-drift`, `.motion-nano-drift-slow`, `.motion-nano-pulse`, `.motion-nano-blink`
- Micro: `.motion-micro-rise`, `.motion-micro-glow`
- Big: `.motion-big-reveal`, `.motion-big-shine`, `.motion-underline-trace`
- Applied: `.motion-nano-drift-slow` on AuditedSeal stamp in hero

---

## Open issues — START HERE in next chat

User said "is k ilawa bhi bht issues agaye site mein" (apart from this, many issues came on site) but did NOT enumerate. Ask user to point them out OR systematically audit each section.

### Known/likely issues based on what I observed in user's screenshots

1. **Live-finding ticker hardcoded** — `HeroLiveFinding` component cycles 6 sample findings ("23 images missing alt-text", etc.) but they're hardcoded. When user runs a real audit and the report shows `IMGS: 0/0`, the ticker still says "23 images missing" — visible inconsistency. Either:
   - Drive the ticker from the real submitted-audit's findings
   - Or label clearly as "example findings" / pause the ticker once a real audit completes
   - File: `src/components/hero-floating-cards.tsx` lines 240-260 (LIVE_TICKS array)

2. **Audit run on Vercel-protected URL returns HTTP 401** — when user pastes the preview URL into the audit, the audit correctly captures `HTTP 401` from Vercel's bypass screen. This is real signal but produces a misleading-looking report (Score 20/100 GRADE F for what's actually a working site). Options:
   - Detect `vercel.app` in the URL and warn the user
   - Strip `?vercel-protection-bypass=` prompt if needed
   - Or: just educate visitors that 401 = preview auth, not site fault
   - Files: `src/app/api/audit/route.ts`, `src/lib/demo-audit.ts` (or wherever the audit runner lives — grep for `scrapeWebsite` or `demo-audit`)

3. **Floating cards may STILL not be visible** — this session I fixed z-index from `-z-[5]` → `z-0` and repositioned cards but did NOT verify the deployed result. User saw the OLDER deploy (`crawliq-jtgkz8hk5...`) without floating cards. The NEWER deploy (`crawliq-dpkj22xj7...`) has the fix but user didn't get to see it yet. **First action in next chat: open the new URL, verify cards render**, then iterate if not.

4. **Scroll perf at length** — earlier commit `51a3a1b` was a perf fix but user added `r3f` 3D scene + 8 floating motion-cards + live-ticker + cursor effects + Reveal scroll observers. Lots of motion. May be janky on lower-end machines. Profile in DevTools.

5. **"Banner" complaint may not be just nav** — user said "ye banner dekho so unprofessional" pointing at the sticky nav. We fixed nav. But user might also dislike:
   - The top chrome bar (`AUDIT REPORT · SUBJECT: CRAWLIQ.IO · MAY 1, 2026 · # REV C3972F0`) at very top
   - The CTA banner section near bottom
   - Confirm with screenshot before assuming.

### Pending from earlier session todos (not addressed)

- **AGENTS-HUB integration (Path C)** — CrawlIQ side ready (commit `b7326a9`), AGENTS-HUB endpoints pending. User needs to paste **3** keys into CrawlIQ env (sandbox can't handle secrets):
  - `AGENTS_HUB_URL` — base URL of AGENTS-HUB Vercel deploy
  - `AGENTS_HUB_PUBLIC_KEY` — rate-limited client key
  - `AGENTS_HUB_INTERNAL_KEY` — server-to-server secret (NEVER expose)
  - When set, CrawlIQ proxies its audit API to AGENTS-HUB instead of running local cheerio+Groq pipeline. Unset = local fallback active.
  - Reference: `project_agents_hub_integration.md` in auto-memory + `docs/AGENTS_HUB_INTEGRATION.md` in repo.
- **Promote latest preview to production** (`vercel deploy --prod`) — not done; user hasn't OK'd. No custom domain attached so prod = Vercel default URL.

### Preview routes status (from earlier punch list in `project_crawliq_pending_work.md`)

These all live at `src/app/preview/<name>/page.tsx`. Earlier session reported them polished but user didn't sign off on each one.

| Route | Reported state | Verify |
|---|---|---|
| `/preview/scroll-hero` | Pyramids replaced with R3F audit-stamps scene | ✓ same scene as homepage now |
| `/preview/watch-think` | Polished | re-check |
| `/preview/features-h` | Was broken — fixed (range guard added in FeatureCard) | re-check |
| `/preview/comparison-scroll` | Polished | re-check |
| `/preview/pricing-3d` | Polished | re-check |
| `/preview/cursor` | Polished + global cursor applied via `GlobalAuditCursor` in layout.tsx | re-check |
| `/preview/section-fx` | Scaling reduced | re-check |
| `/preview/page-load` | Polished | re-check |
| `/preview/audio-hero` | Polished | re-check |
| `/preview/audit-doc` | Polished | re-check |

There are also `/preview/bloomberg`, `/preview/blueprint`, `/preview/magazine`, `/preview/riso` — these are theme exploration routes from earlier (kept for reference).

---

## File map — where things live

```
src/
├── app/
│   ├── layout.tsx              ← Clerk provider, fonts, top chrome bar (cream-aligned)
│   ├── page.tsx                ← composes all home sections
│   ├── globals.css             ← ALL design tokens + utilities (layered shadows, motion, type)
│   ├── api/audit/route.ts      ← audit runner (uses cheerio + Groq)
│   ├── audit/[id]/page.tsx     ← report page
│   ├── preview/                ← isolated route experiments (12 routes)
│   ├── brand/page.tsx          ← brand-identity board
│   └── (app)/                  ← authed dashboard etc
├── components/
│   ├── sections/
│   │   ├── nav.tsx             ← header (cream translucent on scroll, FIXED)
│   │   ├── hero.tsx            ← H1 + form + 3D scene + floating cards (RECENTLY EDITED)
│   │   ├── pricing.tsx         ← 3-tier cards (badge clip FIXED)
│   │   ├── features.tsx, problems.tsx, comparison.tsx,
│   │   ├── case-study.tsx, faq.tsx, pdf-preview.tsx,
│   │   ├── early-access.tsx, data-sources-strip.tsx,
│   │   └── watch-it-think.tsx
│   ├── hero-floating-cards.tsx ← NEW: 8 audit-themed floating cards
│   ├── three-scene-hero.tsx    ← R3F 3D audit-stamps scene
│   ├── audit-stamp.tsx         ← <AuditedSeal/>
│   ├── live-audit-margin.tsx   ← right-rail HUD widget (LCP/CLS/etc real metrics)
│   ├── report-chrome.tsx       ← top "AUDIT REPORT · SUBJECT" bar
│   ├── global-audit-cursor.tsx ← global cursor (orange ring, mounted in layout.tsx)
│   ├── kinetic-heading.tsx     ← word-staggered H1 reveal
│   ├── reveal.tsx              ← framer scroll-into-view wrapper
│   ├── magnetic.tsx            ← magnetic button hover
│   ├── inline-audit.tsx        ← inline audit results panel (used after URL submit)
│   ├── live-ticker.tsx         ← below hero form
│   └── …
├── lib/
│   ├── cn.ts
│   └── (audit runner files — grep for scrapeWebsite)
└── docs/
    ├── AGENTS_HUB_INTEGRATION.md
    └── HANDOFF_2026-05-02.md   ← THIS FILE
```

---

## Communication style

User is direct. Speaks Hinglish ("dekho gor se", "ye wala", "fix kerdo"). Use the same register but stay professional.

He gets visibly frustrated when:
- Work is reported "complete" but the deployed result doesn't show the fix
- I silently revert his locked design decisions (cream → white, tangerine → green, etc.)
- I deliver a menu of options instead of executing
- I add unnecessary error handling, comments, or planning docs

What he WANTS:
- One line saying what I'm about to do
- Action via tool calls
- Final 1-2 sentence status with the deployed URL
- Honest "this is broken / didn't work" instead of false success claims

Use TodoWrite for >3 step tasks. Don't ask which task to do first — just do them in the listed order.

---

## Memory locations

- `~/.claude/projects/-Users-laptopchoice-Projects-Websites-Audit/memory/MEMORY.md` — index
- Existing entries (read these for cross-session context):
  - `project_crawliq_pending_work.md` — previous punch list (mostly addressed)
  - `project_agents_hub_integration.md` — AGENTS-HUB Path C state
  - `feedback_no_fake_audit_results.md` — every finding needs real signal
  - `feedback_human_voice.md` — no AI-rhetorical patterns in copy
  - `user_profile.md` — Umer + DPL/VCS/BSL businesses

---

## Verification commands you can run immediately

```bash
# CRITICAL: get current latest deploy (the URL in this file becomes stale after next push)
cd /Users/laptopchoice/Projects/crawliq && vercel ls | head -5

# git state — handoff file MAY be uncommitted; check before assuming
cd /Users/laptopchoice/Projects/crawliq && git status

# verify build (note: runs `prisma generate` first; needs DATABASE_URL in .env.local)
cd /Users/laptopchoice/Projects/crawliq && pnpm build

# typecheck (faster than full build, no prisma needed)
cd /Users/laptopchoice/Projects/crawliq && pnpm exec tsc --noEmit

# dev server (Turbopack)
cd /Users/laptopchoice/Projects/crawliq && pnpm dev

# deploy preview (no flags = preview, not prod)
cd /Users/laptopchoice/Projects/crawliq && vercel deploy

# promote to prod
cd /Users/laptopchoice/Projects/crawliq && vercel deploy --prod

# inspect a specific deploy
vercel inspect <deploy-url>
```

## Hero z-stacking diagram (debugging the floating cards)

```
<section id="hero" overflow-hidden>          ← stacking context root
  <div bg-radial    -z-10 />                  ← warm radial gradients
  <div bg-grid      -z-10 />                  ← engineering paper grid
  <HeroFloatingCards z-0 />                   ← 8 audit-themed cards (FIXED from -z-[5])
  <div AuditedSeal  z-0 motion-nano-drift-slow />
  <HeroLiveFinding  z-0 absolute top-24 />    ← cycles every 4.2s
  <div content      relative />               ← H1, form, stats — sits above z-0 by default
</section>
```

If cards still don't render after the fix: check that `<HeroFloatingCards>` opens up to `block md:` (it has `hidden md:block`), and confirm parent isn't creating an opaque background that hides z-0 children.

## Audit demo — IMPORTANT

When demoing the live audit, **do NOT paste a Vercel preview URL** — the audit will hit Vercel's deployment-protection screen and return HTTP 401, producing a misleading "Score 20 GRADE F" report. Use a real public URL like `https://digitalpointllc.com` or any normal site. Add a guardrail in `/api/audit/route.ts` that detects `vercel.app` hosts and warns.

## "Stop" pattern

When user types `stop` (or any equivalent: "ruko", "wait", "pause"), STOP all in-flight work immediately. Do not start new tool calls. Wrap up only the current tool call if it's already running.

---

## First message for the next chat — suggested opener

> Resuming from yesterday's session. Latest preview is https://crawliq-dpkj22xj7-umidx124s-projects.vercel.app with floating-cards z-index fix shipped (commit 8c2e19c). You said "many other issues came on site" — point me at the first one (or send a screenshot) and I'll start there. No re-explanation needed; full handoff is in `docs/HANDOFF_2026-05-02.md`.
